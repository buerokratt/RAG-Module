# Eval trigger

Throwaway file to trigger the DeepEval and DeepTeam red-team workflows via a PR
into `wip-eval`. Branch is based on `deepeval-test` so the fixed eval workflow
(Azure judge config) runs. Safe to delete. Do not merge this PR.
